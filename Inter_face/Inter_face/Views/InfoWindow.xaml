<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Inter_face.ViewModel"
        xmlns:local="clr-namespace:Inter_face.Views"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity" xmlns:Custom="http://www.galasoft.ch/mvvmlight" x:Class="Inter_face.Views.InfoWindow"
        mc:Ignorable="d"
        Title="消息窗口" Height="236.331" Width="407.914" ResizeMode="CanMinimize" WindowStyle="ToolWindow" DataContext="{DynamicResource ShowInfosViewModel}">
    <Window.Resources>
        <vm:ShowInfosViewModel x:Key="ShowInfosViewModel"/>
    </Window.Resources>
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Closed">
            <Custom:EventToCommand x:Name="CloseEventToCommand" Command="{Binding UnRegistedCommand}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Grid>
        <TextBox x:Name="InfotextBox" HorizontalAlignment="Left" Height="211" Margin="2,0,0,0" TextWrapping="Wrap" Text="{Binding Infos}" VerticalAlignment="Top" Width="400" IsReadOnly="True"/>
    </Grid>
</Window>
